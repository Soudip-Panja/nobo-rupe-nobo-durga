// Home page — intentionally blank for now (content coming later).
// The themed background lets the petals & gold dust from the Welcome
// transition drift across the page.
export default function Home() {
  return (
    <main
      style={{
        minHeight: '100vh',
        background: 'radial-gradient(ellipse at 50% 30%, #6a0707 0%, #2a0202 60%, #160000 100%)',
      }}
    />
  )
}
